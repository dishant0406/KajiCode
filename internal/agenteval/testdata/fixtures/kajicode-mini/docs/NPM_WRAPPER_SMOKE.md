# NPM Wrapper Smoke

Use Go commands for release validation:

```bash
go run ./cmd/kajicode-release build
go run ./cmd/kajicode-release smoke
```

Use Node only when directly exercising `bin/kajicode.js`.
