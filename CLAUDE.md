# KajiCode

Use [AGENTS.md](AGENTS.md) as the source of truth for repository instructions.

This file exists because the sibling Kaji app exposes its agent instructions
through `CLAUDE.md` and `AGENTS.md`. KajiCode keeps one canonical instruction
document to avoid drift across agent entrypoints.
